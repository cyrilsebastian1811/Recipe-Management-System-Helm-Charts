apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webapp-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "webapp-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webapp-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
